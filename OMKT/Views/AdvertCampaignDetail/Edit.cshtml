@model OMKT.Business.AdvertCampaignDetail

@using (Html.BeginForm("Edit", "AdvertCampaignDetail", FormMethod.Post, new { @class="form-horizontal", @id = "save-detail" }))
{
    @Html.ValidationSummary(true)
    <text>
        @{ Html.RenderPartial("EditOrAddCampaignDetailsPartial", Model); }
    </text>
}
