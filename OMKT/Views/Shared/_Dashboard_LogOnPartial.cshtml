@if(Request.IsAuthenticated) {
    <ul class="nav pull-right">
        <li class="divider-vertical"></li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle"  data-toggle="dropdown">@*<i class="icon-user icon-white"></i>*@@User.Identity.Name <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li><a href="@Url.Action("EditProfile", "Account")">Mi Perfil</a></li>
                <li><a href="@Url.Action("ChangePassword", "Account")">Cambiar contraseña</a></li>
                <li class="divider"></li>
                <li>@Html.ActionLink("Salir", "LogOff", "Account")</li>
            </ul>
        </li>
    </ul>
}
else
{
    @: @Html.ActionLink("Iniciar sesión", "LogOn", "Account") 
}
