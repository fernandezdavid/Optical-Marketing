@model OMKT.Business.AdvertDetail

@{
    ViewBag.Title = "Crear";
}

@using (Html.BeginForm("Create", "CatalogDetails", FormMethod.Post, new { @class="form-horizontal" }))
{
    @Html.ValidationSummary(true)
    <text>
        @{ Html.RenderPartial("EditOrAddCatalogDetailsPartial", Model); }
    </text>
}